# 视觉组第二次考核评分标准

> 本标准依据作业 README 的验收清单与评分要点，结合对五份提交的实际代码阅读制定。  
> 满分 **100 分**，共七个维度。  
> 各小项均标注了"给满分的最低要求"，方便根据实际完成度酌情扣分。

---

## 一、继承体系与功能完整度（30 分）

> 考查 Object → Target / Robot → 各派生类的实现是否完整、逻辑正确。

| 子项 | 分值 | 给满分的最低要求 |
|------|------|-----------------|
| **Object 基类** | 8 | 包含 `id`、位置 `(x,y)`、朝向 `dir`、`movable` 标记；若 `movable=true` 使用 exprtk 驱动 `x/y/dir`；提供 `update(deltaTime)` 更新位置 |
| **Target 抽象类** | 5 | 继承 Object；含 `width`；纯虚 `GetBounds()`（返回端点集合）和 `describe()`；虚析构 |
| **ArmorPlate** | 5 | 继承 Target；含颜色、编号、`ArmorSide`；以 `weak_ptr` 持有宿主 Robot；位置由宿主姿态（底盘旋转）计算而非 YAML 绝对坐标；外法向量用于单面击打判定 |
| **EnergyMechanism** | 3 | 继承 Target；含可激活状态 `isActive`、伤害加成倍数与持续时间（可从 YAML 或全局默认取） |
| **Obstacle** | 2 | 继承 Target；含材质 / 可破坏性属性；提供线段集合供射线遮挡检测 |
| **Robot 基类** | 4 | 多重继承 Object 与 ColoredObject；含长宽、云台相对角 `gimbalAngle`、底盘小陀螺函数；持有 ArmorPlate 列表并提供 `UpdateArmors()`（底盘旋转 → 世界旋转同步装甲板位置） |
| **EnemyRobot** | 3 | 继承 Robot；含 `hp` / `maxHp`；提供 `TakeDamage`（扣血并判断死亡） |

**扣分参考：**
- 类存在但关键字段 / 方法缺失：每项扣 1–2 分  
- 继承关系错误（如 EnemyRobot 同时继承 Robot 和 ArmorPlate）：扣 2–3 分  
- 仅有头文件声明、无任何实现：该项扣 50%  
- 基类与派生类构造函数参数传递错误导致无法编译：扣 1–2 分  

---

## 二、静态成员、函数重载与友元输出（15 分）

| 子项 | 分值 | 给满分的最低要求 |
|------|------|-----------------|
| **SentryRobot 静态成员** | 3 | `static int instanceCount_`（构造/析构自增减）与 `static long long totalTargetsDetected_`；提供查询方法 |
| **函数重载** | 6 | `fire()` 与 `fire(AmmoType)`；`move(length, direction)`；`changeDirection(deltaRad)` 均正确实现且可区分调用 |
| **友元 `operator<<`** | 3 | 在 SentryRobot 中声明 `friend`，并在类外实现，能输出哨兵位置、朝向、伤害加成等摘要信息 |
| **RingBuffer<T, N> 模板** | 3 | 含 `push` / `pop` / `full` / `empty` / `size` 与 `operator[]` 下标访问；`static_assert(N>0)` |

**扣分参考：**
- 静态成员只声明未在 `.cpp` 定义（ODR 违规）：扣 1 分  
- 重载函数签名一致（相同参数列表）导致编译错误：每处扣 1 分  
- RingBuffer 缺少边界检查或下标越界未抛异常：扣 1 分  

---

## 三、YAML 配置与 exprtk 运动（15 分）

| 子项 | 分值 | 给满分的最低要求 |
|------|------|-----------------|
| **YAML 字段解析** | 8 | 能正确解析 `ally_sentry`、`enemy_robots`（含 `armors[]`）、`energy_mechanisms[]`、`obstacles[]`、`global` 等关键字段并用于初始化对象 |
| **缺省配置容错** | 3 | 找不到外部 `config.yaml` 时自动加载内置示例配置，程序可直接运行 |
| **exprtk 运动驱动** | 4 | 正确注册时间变量 `t` 与常量 `pi`，将 YAML 中的表达式字符串编译为 `expression<double>` 并在每次 `update` 时求值更新 `x/y/dir` |

**扣分参考：**
- 未使用 yaml-cpp 而是硬编码参数：YAML 字段解析扣 6–8 分  
- exprtk 编译失败时无错误提示或程序崩溃：扣 1–2 分  
- 只解析部分字段（如缺少 `energy_mechanisms` 或 `obstacles`）：每缺一类扣 1 分  

---

## 四、伤害与血量系统（15 分）

| 子项 | 分值 | 给满分的最低要求 |
|------|------|-----------------|
| **`calculateDamage` 函数** | 5 | 按弹丸类型取基础伤害；包含距离衰减公式（如指数衰减或线性衰减）；提供坐标重载与纯距离重载至少两种签名 |
| **单面击打判定** | 5 | 在 `isArmorHittable` / `isHittableBy` 中计算外法向量，用点积 `< 0` 判断攻击方在装甲外侧 |
| **能量机关伤害加成** | 3 | 命中能量机关可触发 `ActivateDamageBoost(multiplier, duration)`；哨兵发射时将当前加成乘入最终伤害 |
| **血量扣除与死亡判定** | 2 | `TakeDamage` 正确扣除并归零；`isAlive()` 用于筛选存活敌人 |

**扣分参考：**
- 点积判断方向错误（应 `< 0` 而写 `> 0`）：扣 2–3 分  
- 距离衰减公式缺失或无意义（如始终返回固定值）：扣 2–3 分  
- 能量机关加成逻辑未接入主循环：扣 2 分  

---

## 五、STL、智能指针与多线程同步（10 分）

| 子项 | 分值 | 给满分的最低要求 |
|------|------|-----------------|
| **智能指针** | 4 | `unique_ptr` 用于独占所有权（如运动控制器）；`shared_ptr` 管理目标与机器人；`weak_ptr` 持有 ArmorPlate 宿主引用（避免循环引用） |
| **STL 容器** | 2 | `std::vector` 管理目标列表；`std::map` 管理弹药库存 |
| **多线程同步** | 4 | 静态成员（或其他共享数据）使用 `std::mutex` + `std::lock_guard` 保护；析构或测试时不产生数据竞争（如用 `static_mutex_` 保护 `instanceCount`） |

**扣分参考：**
- 循环引用（shared_ptr 相互持有）且未使用 weak_ptr 打破：扣 2 分  
- 静态成员无锁保护：扣 2 分  
- 完全不使用智能指针（全部裸指针）：该子项扣 3–4 分  

---

## 六、现代 C++17/20 特性（10 分）

| 子项 | 分值 | 给满分的最低要求 |
|------|------|-----------------|
| **`std::optional`** | 2 | 用于可能为空的返回值或延迟初始化字段（如云台角配置项） |
| **C++17 其他特性** | 3 | 至少两种：结构化绑定、`if constexpr`、内联变量（`inline static`）、`std::variant`、折叠表达式等 |
| **C++20 特性（可选加分）** | 2 | 使用概念（`concept`）、ranges、三路比较（`<=>`）、`std::format` 或指定初始化器 |
| **`std::format` 兼容性** | 3 | 若使用 `std::format`，提供 `std::ostringstream` 回退方案，或条件编译判断编译器支持 |

**扣分参考：**
- 声称使用某特性但代码中未出现：扣 1–2 分  
- `std::format` 无回退导致编译失败（MSVC 2019 / GCC 10 等旧环境）：扣 2 分  

---

## 七、日志输出与整体运行连贯性（5 分）

| 子项 | 分值 | 给满分的最低要求 |
|------|------|-----------------|
| **可编译运行** | 2 | 代码能在本地或 CI 环境编译并执行（含 cmake / make 流程）；缺省外部配置时自动运行 |
| **命中日志** | 2 | 每次命中输出装甲面、距离、伤害量、目标剩余血量；所有敌人死亡或达上限时输出汇总 |
| **学习报告质量** | 1 | 提交了学习报告（md / txt）；内容与本次任务相关，涵盖所学知识点 |

**扣分参考：**
- 代码无法编译（链接错误 / 语法错误未修复）：可编译运行扣 2 分  
- 无任何运行输出：命中日志扣 2 分  

---

## 各同学初步评分参考

> 以下为基于代码阅读的**初步参考分**，最终评分请结合实际运行结果调整。

| 维度 | 满分 | 武文竣 | 周渝皓 | 杨洵 | 洪嘉源 | 贡丞叶 |
|------|------|--------|--------|------|--------|--------|
| 一、继承体系与功能完整度 | 30 | 22 | 8 | 20 | 26 | 28 |
| 二、静态成员、重载与友元 | 15 | 12 | 2 | 11 | 13 | 14 |
| 三、YAML 配置与 exprtk 运动 | 15 | 4 | 0 | 5 | 12 | 14 |
| 四、伤害与血量系统 | 15 | 8 | 0 | 7 | 14 | 14 |
| 五、STL、智能指针与多线程 | 10 | 7 | 2 | 7 | 8 | 10 |
| 六、现代 C++17/20 特性 | 10 | 4 | 1 | 6 | 7 | 8 |
| 七、日志输出与运行连贯性 | 5 | 3 | 1 | 3 | 5 | 5 |
| **合计** | **100** | **60** | **14** | **59** | **85** | **93** |

### 各同学亮点与扣分说明

#### 032510611 武文竣（参考 60 分）
- **亮点：** 代码结构清晰，头文件 / 源文件分离规范；mathtool.hpp 几何工具完整（角度归一化、距离、旋转、射线相交）；SentryRobot 静态成员、friend operator<< 均有声明；EnergyMechanism 含全局默认 config 结构。  
- **主要扣分：** 未见 YAML 加载逻辑与 main.cpp；target 构造函数未向基类 Object 传递 id/x/y 参数；Object setter 函数命名与 getter 同名（如 `getId(int)`）会产生歧义；ArmorPlate / Obstacle 缺少 `GetBounds()` 与 `describe()` 实现；无法确认代码可编译运行。

#### 周渝皓（参考 14 分）
- **亮点：** 已定义基本类结构；`ArmorPlate::isHit` 有击打方向参数；`target.hpp` 中 ArmorPlate / EnergyMechanism / Obstacle / Robot 框架均有尝试。  
- **主要扣分：** 自述"连 exe 都没有"，代码存在大量编译错误（无 class 的 enum、构造函数默认参数顺序错误、Robot 类重复定义、枚举值与成员函数命名冲突等）；无 YAML / exprtk 集成；无伤害计算；无 main 逻辑；功能远未完整。

#### 杨洵（参考 59 分）
- **亮点：** 使用 namespace `RobotSimulation` 组织代码；引入 `Vector2D` 几何类和 geometry.hpp；SentryRobot 弹药管理（`std::map<AmmoType,int>`）、多种 fire 重载较完整；ArmorPlate 含 `outwardsNormal` 与 `updateWorldPosition`；有学习笔记（C++report.md）。  
- **主要扣分：** `EnemyRobot` 同时继承 `Robot` 和 `ArmorPlate`（继承关系错误）；main.cpp 仅调用 `VisionTest::run()`，而 VisionTest 类未见实现；未见 YAML 加载；exprtk 运动驱动逻辑未在提交代码中体现；RingBuffer 未见。

#### 洪嘉源（参考 85 分）
- **亮点：** 提交最为完整；单文件 VisionTest2.h/cpp 包含所有类，代码可直接编译运行；RingBuffer 模板实现正确；`calculateDamage` 三种重载；ArmorPlate 世界坐标、外法向量、`isHittableBy` 判定均完整；YAML 加载+内置默认配置容错；主循环 2000 次+1ms 冷却；命中日志完整。  
- **主要扣分：** Object 静态 nextId 方案未考虑多实例 id 分配；`UpdateArmors()` 仅重设 host 引用而未重新计算装甲板世界坐标；现代 C++ 特性使用较少（无 `std::optional` / 结构化绑定等）；多线程仅用 `std::thread::sleep`，无加锁保护静态成员。

#### 贡丞叶（参考 93 分）
- **亮点：** 代码模块化程度最高，每个类独立 .h/.cpp 文件；Config 单例模式 + YAML 加载最完整；SentryRobot 静态成员使用 `std::mutex` 保护（线程安全）；GameSimulation 封装清晰；DamageCalculator 独立；report.md + report2.md 学习报告详尽。  
- **主要扣分：** RingBuffer 模板未在提交代码中找到；现代 C++17 特性（`std::optional` 有少量使用，但 `std::format` / 结构化绑定等未见）；能量机关伤害加成在主循环的接入逻辑可再完善。

---

## 附：评分操作建议

1. **先行编译验证：** 对能提供完整 CMakeLists.txt 的同学（武文竣、杨洵、洪嘉源、贡丞叶）尝试编译，编译失败按"无法编译运行"扣分。  
2. **运行输出核验：** 检查主循环是否按最近装甲板射击、日志是否含距离 / 伤害 / 血量字段。  
3. **代码静态分析：** 重点检查 ArmorPlate 外法向量方向正确性（单面 vs 双面）与 YAML 字段名是否与 README 一致。  
4. **报告加成：** 学习报告内容与代码实现高度一致（非纯抄概念）可在第七项给满分。  
5. **最终分数调整：** 本表中"初步参考分"仅供参考，请按实际运行效果与代码质量综合判断。

---

*评分标准版本：v1.0，制定时间：2025-03，如有修改请更新版本号。*
